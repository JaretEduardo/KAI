<UserControl x:Class="KAI_UI.Views.ModelsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="650" d:DesignWidth="950"
             Background="Transparent"
             Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <Style x:Key="ScrollThumb" TargetType="{x:Type Thumb}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Border x:Name="thumbBorder" Background="#3300D4FF" CornerRadius="4"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="thumbBorder" Property="Background" Value="#FF00D4FF"/>
                                <Setter TargetName="thumbBorder" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#00D4FF" BlurRadius="8" ShadowDepth="0"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="thumbBorder" Property="Background" Value="#FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type ScrollBar}">
            <Setter Property="Stylus.IsFlicksEnabled" Value="false" />
            <Setter Property="Foreground" Value="#3300D4FF" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Width" Value="8" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid x:Name="GridRoot" Width="8" Background="{TemplateBinding Background}">
                            <Track x:Name="PART_Track" IsDirectionReversed="true" Focusable="false">
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource ScrollThumb}" />
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NeonCardStyle" TargetType="Border">
            <Setter Property="Background" Value="#CC09101A"/>
            <Setter Property="BorderBrush" Value="#3300D4FF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="15"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" BlurRadius="20" ShadowDepth="5" Opacity="0.6"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#00D4FF"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderBrush" Value="#00D4FF"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="20">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,8"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2200D4FF"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#00D4FF" BlurRadius="15" ShadowDepth="0"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="18">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,6"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#22FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SelectButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#00D4FF"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderBrush" Value="#00D4FF"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="14">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="18,4"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#00D4FF"/>
                                <Setter Property="Foreground" Value="Black"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#00D4FF" BlurRadius="10" ShadowDepth="0"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DangerButtonStyle" TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
            <Setter Property="Foreground" Value="#FF4444"/>
            <Setter Property="BorderBrush" Value="#FF4444"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="18">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,6"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#22FF4444"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#FF4444" BlurRadius="15" ShadowDepth="0"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF4444"/>
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="30,20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="380"/>
            <ColumnDefinition Width="30"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Style="{StaticResource NeonCardStyle}" Padding="20">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="Available Models" Foreground="#E0E0E0" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,20"/>

                <ListBox ItemsSource="{Binding Models}" 
                         SelectedItem="{Binding SelectedModel}"
                         Background="Transparent" 
                         BorderThickness="0" 
         
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         ScrollViewer.VerticalScrollBarVisibility="Visible" 
                         VirtualizingPanel.ScrollUnit="Pixel">

                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Margin" Value="0,0,0,15"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="CardBorder" 
            BorderThickness="1" 
            CornerRadius="12" 
            Height="90">

                                            <Border.Background>
                                                <SolidColorBrush Color="#0A0F16"/>
                                            </Border.Background>

                                            <Border.BorderBrush>
                                                <SolidColorBrush Color="#3300D4FF"/>
                                            </Border.BorderBrush>

                                            <VisualStateManager.VisualStateGroups>
                                                <VisualStateGroup x:Name="SelectionStates">
                                                    <VisualState x:Name="Unselected" />
                                                    <VisualState x:Name="Selected">
                                                        <Storyboard>
                                                            <ColorAnimation Storyboard.TargetName="CardBorder" 
                                        Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)" 
                                        To="#00D4FF" Duration="0:0:0.2"/>
                                                            <ColorAnimation Storyboard.TargetName="CardBorder" 
                                        Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)" 
                                        To="#1500D4FF" Duration="0:0:0.2"/>
                                                        </Storyboard>
                                                    </VisualState>
                                                </VisualStateGroup>
                                            </VisualStateManager.VisualStateGroups>

                                            <ContentPresenter />
                                        </Border>

                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="CardBorder" Property="Effect">
                                                    <Setter.Value>
                                                        <DropShadowEffect Color="#00D4FF" BlurRadius="20" ShadowDepth="0" Opacity="0.6"/>
                                                    </Setter.Value>
                                                </Setter>
                                            </Trigger>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="CardBorder" Property="BorderBrush">
                                                    <Setter.Value>
                                                        <SolidColorBrush Color="#8800D4FF"/>
                                                    </Setter.Value>
                                                </Setter>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>

                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="15,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <Path Grid.Column="0" Data="M12,2A10,10 0 0,0 2,12A10,10 0 0,0 12,22A10,10 0 0,0 22,12A10,10 0 0,0 12,2M12,4A8,8 0 0,1 20,12A8,8 0 0,1 12,20A8,8 0 0,1 4,12A8,8 0 0,1 12,4M12,6A6,6 0 0,0 6,12A6,6 0 0,0 12,18A6,6 0 0,0 18,12A6,6 0 0,0 12,6M12,8A4,4 0 0,1 16,12A4,4 0 0,1 12,16A4,4 0 0,1 8,12A4,4 0 0,1 12,8Z" 
                                      Fill="#00D4FF" Width="34" Height="34" Stretch="Uniform" VerticalAlignment="Center" Margin="0,0,15,0">
                                    <Path.Effect>
                                        <DropShadowEffect Color="#00D4FF" BlurRadius="6"/>
                                    </Path.Effect>
                                </Path>

                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Name}" Foreground="White" FontWeight="SemiBold" FontSize="15" Margin="0,0,0,3"/>
                                    <TextBlock Text="{Binding Metrics.FinalAccuracy, StringFormat='Accuracy: {0:P1}'}" Foreground="#E0E0E0" FontSize="12"/>
                                    <TextBlock Text="{Binding CreatedAt, StringFormat='Last trained: {0:M/d/yy}'}" Foreground="#708090" FontSize="11" Margin="0,2,0,0"/>
                                </StackPanel>

                                <Button Grid.Column="2" Content="Select" Style="{StaticResource SelectButtonStyle}" VerticalAlignment="Center" IsHitTestVisible="False"/>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </Border>

        <Border Grid.Column="2" Style="{StaticResource NeonCardStyle}" Padding="30">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,20">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                        <TextBlock Text="Model " Foreground="White" FontSize="18"/>
                        <TextBlock Text="{Binding SelectedModel.Name}" Foreground="White" FontWeight="Bold" FontSize="18"/>
                        <TextBlock Text=" - Details" Foreground="White" FontSize="18"/>
                    </StackPanel>
                    <TextBlock Text="Performance Metrics" Foreground="#E0E0E0" FontSize="14"/>
                </StackPanel>

                <Grid Grid.Row="1" Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="30"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <DockPanel Margin="0,0,0,8">
                            <TextBlock Text="Accuracy" Foreground="#B0C4DE" FontSize="13"/>
                            <TextBlock Text="{Binding SelectedModel.Metrics.FinalAccuracy, StringFormat='{}{0:P1}'}" Foreground="White" FontWeight="Bold" HorizontalAlignment="Right" FontSize="13"/>
                        </DockPanel>

                        <Border Height="80" BorderBrush="#22FFFFFF" BorderThickness="0,0,0,1" Background="#05FFFFFF">
                            <Grid ClipToBounds="True">
                                <Path Data="M0,0.5 L1,0.5" Stretch="Fill" Stroke="#11FFFFFF" StrokeThickness="1" StrokeDashArray="2 2" VerticalAlignment="Center"/>
                                <Viewbox Stretch="Fill">
                                    <Grid Width="100" Height="100">
                                        <Polygon Points="{Binding AccFillPoints}" StrokeThickness="0">
                                            <Polygon.Fill>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                    <GradientStop Color="#4000D4FF" Offset="0"/>
                                                    <GradientStop Color="#0000D4FF" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Polygon.Fill>
                                        </Polygon>
                                        <Polyline Points="{Binding AccPoints}" Stroke="#00D4FF" StrokeThickness="1.5"/>
                                    </Grid>
                                </Viewbox>
                            </Grid>
                        </Border>

                        <DockPanel Margin="0,4,0,0">
                            <TextBlock Text="0" Foreground="#556677" FontSize="10"/>
                            <TextBlock Text="{Binding SelectedModel.Hyperparameters.Epochs}" Foreground="#556677" FontSize="10" HorizontalAlignment="Right"/>
                        </DockPanel>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <DockPanel Margin="0,0,0,8">
                            <TextBlock Text="Loss" Foreground="#B0C4DE" FontSize="13"/>
                            <TextBlock Text="{Binding SelectedModel.Metrics.FinalLoss, StringFormat='{}{0:F3}'}" Foreground="White" FontWeight="Bold" HorizontalAlignment="Right" FontSize="13"/>
                        </DockPanel>

                        <Border Height="80" BorderBrush="#22FFFFFF" BorderThickness="0,0,0,1" Background="#05FFFFFF">
                            <Grid ClipToBounds="True">
                                <Path Data="M0,0.5 L1,0.5" Stretch="Fill" Stroke="#11FFFFFF" StrokeThickness="1" StrokeDashArray="2 2" VerticalAlignment="Center"/>
                                <Viewbox Stretch="Fill">
                                    <Grid Width="100" Height="100">
                                        <Polygon Points="{Binding LossFillPoints}" StrokeThickness="0">
                                            <Polygon.Fill>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                    <GradientStop Color="#408080FF" Offset="0"/>
                                                    <GradientStop Color="#008080FF" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Polygon.Fill>
                                        </Polygon>
                                        <Polyline Points="{Binding LossPoints}" Stroke="#8080FF" StrokeThickness="1.5"/>
                                    </Grid>
                                </Viewbox>
                            </Grid>
                        </Border>

                        <DockPanel Margin="0,4,0,0">
                            <TextBlock Text="0" Foreground="#556677" FontSize="10"/>
                            <TextBlock Text="{Binding SelectedModel.Hyperparameters.Epochs}" Foreground="#556677" FontSize="10" HorizontalAlignment="Right"/>
                        </DockPanel>
                    </StackPanel>
                </Grid>

                <StackPanel Grid.Row="2" Margin="0,0,0,20">
                    <TextBlock Text="Architecture Overview" Foreground="#E0E0E0" FontSize="14" Margin="0,0,0,15"/>
                    <Viewbox Height="65" HorizontalAlignment="Left">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Border Width="16" Height="60" BorderBrush="#00D4FF" BorderThickness="1" CornerRadius="3" Background="#1100D4FF" Margin="0,0,8,0">
                                <TextBlock Text="Conv2D" Foreground="#00D4FF" FontSize="7" VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <TextBlock.LayoutTransform>
                                        <RotateTransform Angle="-90"/>
                                    </TextBlock.LayoutTransform>
                                </TextBlock>
                            </Border>
                            <Path Data="M0,0 L4,4 L0,8" Stroke="#445566" StrokeThickness="1" VerticalAlignment="Center" Margin="0,0,8,0"/>

                            <Border Width="16" Height="40" BorderBrush="#8080FF" BorderThickness="1" CornerRadius="3" Background="#118080FF" Margin="0,0,8,0">
                                <TextBlock Text="MaxPool" Foreground="#8080FF" FontSize="7" VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <TextBlock.LayoutTransform>
                                        <RotateTransform Angle="-90"/>
                                    </TextBlock.LayoutTransform>
                                </TextBlock>
                            </Border>
                            <Path Data="M0,0 L4,4 L0,8" Stroke="#445566" StrokeThickness="1" VerticalAlignment="Center" Margin="0,0,8,0"/>

                            <Border Width="16" Height="50" BorderBrush="#00D4FF" BorderThickness="1" CornerRadius="3" Background="#1100D4FF" Margin="0,0,8,0">
                                <TextBlock Text="Conv2D" Foreground="#00D4FF" FontSize="7" VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <TextBlock.LayoutTransform>
                                        <RotateTransform Angle="-90"/>
                                    </TextBlock.LayoutTransform>
                                </TextBlock>
                            </Border>
                            <Path Data="M0,0 L4,4 L0,8" Stroke="#445566" StrokeThickness="1" VerticalAlignment="Center" Margin="0,0,8,0"/>

                            <Border Width="16" Height="30" BorderBrush="#8080FF" BorderThickness="1" CornerRadius="3" Background="#118080FF" Margin="0,0,8,0">
                                <TextBlock Text="MaxPool" Foreground="#8080FF" FontSize="7" VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <TextBlock.LayoutTransform>
                                        <RotateTransform Angle="-90"/>
                                    </TextBlock.LayoutTransform>
                                </TextBlock>
                            </Border>
                            <Path Data="M0,0 L4,4 L0,8" Stroke="#445566" StrokeThickness="1" VerticalAlignment="Center" Margin="0,0,8,0"/>

                            <Border Width="12" Height="60" BorderBrush="#B080FF" BorderThickness="1" CornerRadius="3" Background="#11B080FF">
                                <TextBlock Text="FC" Foreground="#B080FF" FontSize="7" VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <TextBlock.LayoutTransform>
                                        <RotateTransform Angle="-90"/>
                                    </TextBlock.LayoutTransform>
                                </TextBlock>
                            </Border>
                        </StackPanel>
                    </Viewbox>
                </StackPanel>

                <Grid Grid.Row="3" Margin="0,0,0,25">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Training History" Foreground="#E0E0E0" FontSize="14" Margin="0,0,0,8"/>

                    <Border Grid.Row="1" Background="#0A0F16" CornerRadius="8" Padding="5">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" PanningMode="VerticalOnly">
                            <ItemsControl ItemsSource="{Binding HistoryLogs}" Margin="5">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}" Foreground="#4080A0" FontFamily="Consolas" FontSize="11" Margin="0,0,0,3"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Border>
                </Grid>

                <StackPanel Grid.Row="4">
                    <Button Content="DEPLOY MODEL" Style="{StaticResource PrimaryButtonStyle}" Height="45" Margin="0,0,0,15"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0" Content="RE-TRAIN" Command="{Binding RetrainCommand}" Style="{StaticResource SecondaryButtonStyle}" Height="40"/>
                        <Button Grid.Column="2" Content="DELETE" Command="{Binding DeleteModelCommand}" Style="{StaticResource DangerButtonStyle}" Height="40"/>
                    </Grid>
                </StackPanel>

            </Grid>
        </Border>
    </Grid>
</UserControl>