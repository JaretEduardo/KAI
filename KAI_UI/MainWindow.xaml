<Window x:Class="KAI_UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="KAI Engine | v0.1.0" Height="720" Width="1280"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundDark}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="{StaticResource PanelDark}" BorderBrush="#333" BorderThickness="0,0,1,0">
            <StackPanel Margin="20">
                <TextBlock Text="KAI" FontSize="32" FontWeight="Bold" Foreground="{StaticResource AccentColor}"/>
                <TextBlock Text="ARTIFICIAL INTELLIGENCE" FontSize="10" Foreground="{StaticResource TextSecondary}" Margin="2,-5,0,30"/>

                <Button Content="🏠 Dashboard" Style="{StaticResource CyberButton}" HorizontalAlignment="Stretch" Margin="0,0,0,10" Background="{StaticResource AccentColor}"/>
                <Button Content="🧠 Modelos" Style="{StaticResource CyberButton}" HorizontalAlignment="Stretch" Margin="0,0,0,10"/>
                <Button Content="⚙️ Configuración" Style="{StaticResource CyberButton}" HorizontalAlignment="Stretch" Margin="0,0,0,10"/>
            </StackPanel>
        </Border>

        <Grid Grid.Column="1" Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right">
                <Border Background="#2D2D30" CornerRadius="15" Padding="15,5" Margin="10,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="GPU: " Foreground="{StaticResource TextSecondary}"/>
                        <TextBlock x:Name="TxtHardwareStatus" Text="Detectando..." FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                    </StackPanel>
                </Border>
            </StackPanel>

            <TextBlock Grid.Row="0" Text="Nuevo Proyecto de Entrenamiento" FontSize="24" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center"/>

            <Border Grid.Row="1" Margin="0,30,0,0" Background="{StaticResource PanelDark}" CornerRadius="10" BorderBrush="#333" BorderThickness="1">
                <Grid>
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="📂" FontSize="50" HorizontalAlignment="Center" Foreground="{StaticResource TextSecondary}"/>
                        <TextBlock Text="Arrastra tu carpeta aquí" FontSize="18" Foreground="{StaticResource TextSecondary}" HorizontalAlignment="Center" Margin="0,10"/>
                        <TextBlock Text="o" FontSize="14" Foreground="#555" HorizontalAlignment="Center" Margin="0,5"/>

                        <Button x:Name="BtnSelectFolder" Content="Explorar Archivos" 
                                Style="{StaticResource CyberButton}" 
                                Width="200" Margin="0,15,0,0"
                                Click="BtnSelectFolder_Click"/>

                        <TextBlock x:Name="TxtAnalysisResult" 
                                   Text="" 
                                   FontSize="16" 
                                   Foreground="{StaticResource AccentColor}" 
                                   HorizontalAlignment="Center" 
                                   Margin="0,20,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>